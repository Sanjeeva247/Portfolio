# Required — MongoDB connection string (get from MongoDB Atlas)
MONGO_URI=mongodb+srv://sanjeevakumarm945_db_user:<db_password>@cluster0.pgqqtyg.mongodb.net/?appName=Cluster0

# Required — Name of the MongoDB database
DB_NAME=portfolio

# Required — Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Optional — SMTP configuration for email notifications (omit all to disable)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=sanjeevakumarm945@gmail.com
SMTP_PASSWORD=ngek auje seux crsa
NOTIFY_EMAIL=sanjeevakumarm945@gmail.com
